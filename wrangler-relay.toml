# 融合网络-OpenAI中转（worker_openai.js）
name = "openai-relay"
main = "worker_openai.js"
compatibility_date = "2024-08-01"

[[kv_namespaces]]
binding = "KV"
id = "替换为你的_KV_namespace_id"

[triggers]
# 每 30 分钟自动探测上游可用模型
crons = ["*/30 * * * *"]

# 环境变量在 Cloudflare 控制台设置：PASSWORD、OPENAI_API_KEY 等
